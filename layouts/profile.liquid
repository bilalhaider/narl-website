---
layout: "master"
---
<section>
    <div class="container">
        <div class="row">
            <div class="col-sm-12 detail">
                <img src="{{page.url | url}}{{profile.picture}}" />
                <h5>{{profile.name}}</h5>
                <p>{{profile.role}}</p>
                {{profile.designation}}<br>
                {{profile.department}}<br>
                {{profile.qualification}}<br>
                {{profile.email}}<br>
                {{profile.phone}}<br><br>
                {{profile.bio}}
            </div>
        </div>
    </div>
<section>